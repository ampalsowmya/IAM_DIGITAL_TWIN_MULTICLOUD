name: CI

on: [push]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Project
        run: |
          echo "IAM Digital Twin CI/CD pipeline running"
          echo "Checking project structure"
          ls -la
          echo "Pipeline completed successfully"
